const PLATFORM_EXAMPLES = {mt5:{config:{terminal_path:'C:/Program Files/MetaTrader 5/terminal64.exe',server:'Broker-Server',stop_pct:0.01,take_pct:0.02,deviation:20,market_type:'forex'},secrets:{login:12345678,password:'broker-password',server:'Broker-Server'}},ctrader:{config:{bridge_url:'http://127.0.0.1:9001',default_quantity:1000,market_type:'forex'},secrets:{client_id:'...',client_secret:'...',access_token:'...',account_id:'...'}},tradingview:{config:{passphrase:'mi-clave-webhook',market_type:'signals'},secrets:{}},binance:{config:{sandbox:false,default_quantity:0.001,stop_pct:0.01,market_type:'spot'},secrets:{api_key:'...',secret_key:'...'}},bybit:{config:{sandbox:false,default_quantity:0.001,stop_pct:0.01,market_type:'spot'},secrets:{api_key:'...',secret_key:'...'}},okx:{config:{sandbox:false,default_quantity:0.001,stop_pct:0.01,market_type:'spot'},secrets:{api_key:'...',secret_key:'...',passphrase:'...'}}};
const PLATFORM_MARKET_TYPES={mt5:['forex','cfd'],ctrader:['forex','cfd','spot','futures'],tradingview:['signals'],binance:['spot','futures'],bybit:['spot','futures'],okx:['spot','futures']};
let METADATA={platforms:[]};
async function api(url,options={}){const res=await fetch(url,{headers:{'Content-Type':'application/json'},credentials:'same-origin',...options});if(!res.ok) throw new Error(await res.text());return res.json();}
function parseJsonSafe(value,fallback={}){if(!value||!value.trim()) return fallback;try{return JSON.parse(value);}catch{return fallback;}}
function parseCsv(value){return value.split(',').map(x=>x.trim()).filter(Boolean);}
function getPlatformMeta(platform){return METADATA.platforms.find(p=>p.platform===platform);}
function renderMarketTypeSelect(platform){const select=document.getElementById('market-type-select');if(!select) return;const marketTypes=PLATFORM_MARKET_TYPES[platform]||['spot'];select.innerHTML=marketTypes.map(mt=>`<option value="${mt}">${mt}</option>`).join('');}
function setPlatformExample(platform){const ex=PLATFORM_EXAMPLES[platform]||{config:{},secrets:{}};const meta=getPlatformMeta(platform);const configEl=document.getElementById('config-json');const secretsEl=document.getElementById('secrets-json');const exampleEl=document.getElementById('connector-example');renderMarketTypeSelect(platform);const marketType=document.getElementById('market-type-select')?.value||(PLATFORM_MARKET_TYPES[platform]||['spot'])[0];ex.config.market_type=marketType;if(configEl&&!configEl.value.trim()) configEl.value=JSON.stringify(ex.config,null,2);if(secretsEl&&!secretsEl.value.trim()) secretsEl.value=JSON.stringify(ex.secrets,null,2);if(exampleEl){exampleEl.textContent=`config = ${JSON.stringify(ex.config,null,2)}\n\nsecrets = ${JSON.stringify(ex.secrets,null,2)}`;}renderSymbolPresets(platform);if(meta){document.getElementById('symbol-limit-hint').textContent=`Máx. símbolos permitidos: ${meta.grant.max_symbols}. Manual: ${meta.allow_manual_symbols?'sí':'no'}.`;}}
function renderPlatformSelect(){const select=document.getElementById('platform-select');if(!select) return;const enabled=METADATA.platforms.filter(p=>p.is_enabled_global&&p.grant?.is_enabled);select.innerHTML=enabled.map(p=>`<option value="${p.platform}">${p.display_name}</option>`).join('');setPlatformExample(enabled[0]?.platform||'mt5');}
function renderSymbolPresets(platform){const wrap=document.getElementById('symbol-preset-list');if(!wrap) return;const meta=getPlatformMeta(platform);const symbols=meta?.top_symbols||[];wrap.innerHTML=symbols.map(s=>`<button type="button" class="chip" data-symbol="${s}">${s}</button>`).join('');wrap.querySelectorAll('.chip').forEach(btn=>btn.addEventListener('click',()=>addSymbol(btn.dataset.symbol)));}
function addSymbol(symbol){const input=document.getElementById('symbols-input');const current=new Set(parseCsv(input.value||''));const platform=document.getElementById('platform-select').value;const meta=getPlatformMeta(platform);const limit=meta?.grant?.max_symbols||0;if(limit&&current.size>=limit&&!current.has(symbol)){alert(`Tu límite en ${platform} es de ${limit} símbolos.`);return;}current.add(symbol);input.value=Array.from(current).join(',');}
function openGuideModal(platform){const meta=getPlatformMeta(platform);if(!meta) return;const guide=meta.guide||{};document.getElementById('guide-title').textContent=guide.title||meta.display_name;document.getElementById('guide-summary').textContent=guide.summary||'';document.getElementById('guide-fields').innerHTML=(guide.fields_needed||[]).map(x=>`<li>${x}</li>`).join('');document.getElementById('guide-steps').innerHTML=(guide.steps||[]).map(x=>`<li>${x}</li>`).join('');document.getElementById('guide-modal').classList.remove('hidden');}
window.closeGuideModal=function(){document.getElementById('guide-modal').classList.add('hidden');};
async function refreshDashboard(){const [connectors,summary,trades]=await Promise.all([api('/api/connectors'),api('/api/dashboard'),api('/api/trades')]);document.getElementById('stat-connectors').textContent=summary.total_connectors;document.getElementById('stat-enabled').textContent=summary.enabled_connectors;document.getElementById('stat-trades').textContent=summary.total_trades;document.getElementById('stat-pnl').textContent=summary.realized_pnl;const limits=document.getElementById('limits-list');limits.innerHTML=(summary.limits||[]).map(l=>`<div class="connector-item"><strong>${l.platform}</strong><div class="connector-meta"><span>enabled: ${l.enabled}</span><span>máx. símbolos: ${l.max_symbols}</span><span>máx. mov/día: ${l.max_daily_movements}</span></div><small class="hint">${l.notes||'Sin notas'}</small></div>`).join('');const list=document.getElementById('connectors-list');list.innerHTML=connectors.map(c=>`<div class="connector-item"><strong>${c.label}</strong><div class="connector-meta"><span>${c.platform}</span><span>mercado: ${c.market_type||'spot'}</span><span>modo: ${c.mode}</span><span>enabled: ${c.is_enabled}</span><span>symbols: ${(c.symbols||[]).join(', ')||'-'}</span></div><div style="margin-top:10px;display:flex;gap:8px;flex-wrap:wrap"><button class="btn" onclick="testConnector(${c.id})">Test</button><button class="btn" onclick="deleteConnector(${c.id})">Eliminar</button></div><pre class="mini-pre">${JSON.stringify(c.config||{},null,2)}</pre></div>`).join('');const tbody=document.querySelector('#trades-table tbody');tbody.innerHTML=trades.map(t=>`<tr><td>${new Date(t.created_at).toLocaleString()}</td><td>${t.platform}</td><td>${(t.meta&&t.meta.market_type)||'-'}</td><td>${t.symbol}</td><td>${t.side}</td><td>${t.quantity}</td><td>${Number(t.price).toFixed(4)}</td><td>${t.status}</td><td>${t.pnl}</td></tr>`).join('');const labels=Object.keys(summary.platforms||{});const values=Object.values(summary.platforms||{});const canvas=document.getElementById('platform-chart');if(window.platformChart) window.platformChart.destroy();window.platformChart=new Chart(canvas,{type:'doughnut',data:{labels,datasets:[{data:values}]},options:{plugins:{legend:{labels:{color:'#f4f7fb'}}}}});}
async function testConnector(id){const out=await api(`/api/connectors/${id}/test`,{method:'POST'});alert(`${out.status}: ${out.message}\n\n${JSON.stringify(out.raw,null,2)}`);}
async function deleteConnector(id){await api(`/api/connectors/${id}`,{method:'DELETE'});refreshDashboard();}
document.getElementById('platform-select')?.addEventListener('change',e=>{document.getElementById('config-json').value='';document.getElementById('secrets-json').value='';setPlatformExample(e.target.value);});
document.getElementById('market-type-select')?.addEventListener('change',()=>{document.getElementById('config-json').value='';setPlatformExample(document.getElementById('platform-select').value);});
document.getElementById('open-guide-btn')?.addEventListener('click',()=>{const platform=document.getElementById('platform-select').value;openGuideModal(platform);});
document.getElementById('connector-form')?.addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.target);const config=parseJsonSafe(fd.get('config'),{});config.market_type=fd.get('market_type');await api('/api/connectors',{method:'POST',body:JSON.stringify({platform:fd.get('platform'),label:fd.get('label'),mode:fd.get('mode'),market_type:fd.get('market_type'),symbols:parseCsv(fd.get('symbols')),config,secrets:parseJsonSafe(fd.get('secrets'),{})})});e.target.reset();renderPlatformSelect();refreshDashboard();});
document.getElementById('run-form')?.addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.target);const result=await api('/api/strategies/run',{method:'POST',body:JSON.stringify({connector_ids:parseCsv(fd.get('connector_ids')).map(Number),symbols:parseCsv(fd.get('symbols')),timeframe:fd.get('timeframe'),strategy_slug:fd.get('strategy_slug'),risk_per_trade:Number(fd.get('risk_per_trade')),min_ml_probability:Number(fd.get('min_ml_probability')),use_live_if_available:fd.get('use_live_if_available')==='on'})});document.getElementById('run-output').textContent=JSON.stringify(result,null,2);refreshDashboard();});
async function init(){METADATA=await api('/api/platform-metadata');renderPlatformSelect();await refreshDashboard();}
init().catch(err=>{console.error(err);const out=document.getElementById('run-output');if(out) out.textContent='Error cargando dashboard: '+err.message;});